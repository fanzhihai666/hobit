server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true
  address: 0.0.0.0

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/habit_checkin?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Fzh246810.
#  redis:
#    host: localhost
#    port: 6379
#    password:
#    database: 0
#    timeout: 3000ms
#    lettuce:
#      pool:
#        max-active: 8
#        max-idle: 8
#        min-idle: 0

jwt:
  secret: ${JWT_SECRET:habit-app-secret-key-2024}
  expiration: 604800000

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.habit.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

upload:
  path: ./upload/avatars/

image:
  access-path: /avatars/

logging:
  level:
    root: info
    com.habit: debug
